__pycache__/
*.pyc
*.pyo
.venv/
venv/
runs/
build/
devel/
install/
logs/
.catkin_tools/

# uv: keep pyproject.toml + .python-version under VCS, but the resolved
# lockfile is environment-specific (cuda vs cpu wheels, etc.) — skip it for now.
uv.lock

# Sample videos are not tracked.
pooh/*.mp4
yolov8x-worldv2.pt
