ultralytics>=8.4.45
# ROS 1's cv_bridge is built against numpy 1.x. Letting pip pull numpy 2 into
# this virtualenv breaks its C ABI at import time (seen on Ubuntu 24.04).
numpy<2
# ByteTrack (dog_pose_node.py ~track) needs a linear-assignment solver.
lap>=0.5.13
clip @ git+https://github.com/openai/CLIP.git
